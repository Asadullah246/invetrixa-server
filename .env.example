
# =================================================================
# ENVIRONMENT FILE TEMPLATE
# =================================================================
#
# USAGE: This project uses a STRICT single-file approach per environment.
#
# Environment Files:
#   .env.development  - Development environment (Docker/local)
#   .env.test         - Test environment (unit/integration tests)
#   .env.production   - Production environment (NOT committed, create manually)
#
# The app loads ONLY ONE file based on NODE_ENV:
#   - NODE_ENV=development -> loads .env.development
#   - NODE_ENV=test        -> loads .env.test
#   - NODE_ENV=production  -> loads .env.production
#
# NO FALLBACKS: If a variable is missing, the app will FAIL at startup.
#
# =================================================================

# =================== APP ========================= #
NODE_ENV=development
PORT=8080

# =================== DATABASE ========================= #
# For Docker Compose, use these to construct DATABASE_URL
DB_NAME=pxlhut
DB_USER=postgres
DB_PASSWORD=password
DB_PORT=5432

DATABASE_URL=postgresql://postgres:password@postgres:5432/pxlhut?schema=public

# =================== FRONTEND URLS ========================= #
FRONTEND_URL=http://localhost:3000
CLIENT_ORIGIN=http://localhost:3000
DASHBOARD_ORIGIN=http://localhost:3000

# =================== SESSION ========================= #
SESSION_SECRET=supersecret32characterslongsessionsecret!!
SESSION_TTL_SECONDS=6048000
SESSION_COOKIE_SAMESITE=lax

# =================== CSRF ========================= #
CSRF_SECRET=your-complex-secret-key-change-in-production
CSRF_SECRET_COOKIE_NAME=_csrf
CSRF_TOKEN_COOKIE_NAME=XSRF-TOKEN
CSRF_HEADER_NAME=x-csrf-token
CSRF_COOKIE_SAMESITE=lax
CSRF_TOKEN_TTL_SECONDS=3600

# =================== REDIS ========================= #
# Option 1: Full URL (recommended)
REDIS_URL=redis://localhost:6379/0

# Option 2: Individual settings (if not using REDIS_URL)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_TLS=false

# =================== THROTTLING ========================= #
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=10000

# =================== TIMEOUT ========================= #
REQUEST_TIMEOUT_MS=10000

# =================== EMAIL (SMTP) ========================= #
EMAIL_SERVICE=gmail
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
