generator client {
  provider      = "prisma-client"
  output        = "../../generated/prisma"
  moduleFormat  = "cjs"
  engine        = "cline"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
}
